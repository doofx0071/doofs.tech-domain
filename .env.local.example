# Deployment URL from Convex Dashboard
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# GitHub OAuth (Set these in Convex Dashboard via `npx convex env set ...`, not here for production usually, but for local dev maybe?)
# Actually Convex Auth reads these from Backend Environment Variables.
# So you run:
# npx convex env set AUTH_GITHUB_ID=...
# npx convex env set AUTH_GITHUB_SECRET=...
