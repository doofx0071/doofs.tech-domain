# ===========================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env.local and fill in your values

# Convex Deployment URL (from Convex Dashboard)
VITE_CONVEX_URL=https://your-deployment-name.convex.cloud

# Cloudflare Turnstile site key (public, safe for frontend)
VITE_TURNSTILE_SITE_KEY=your-turnstile-site-key

# Admin Panel Route (optional - defaults to /admin if not set)
# Use a non-obvious path for security through obscurity
# Note: This is visible in client JS bundle, real security is via backend role checks
VITE_ADMIN_ROUTE=/admin-122303

# ===========================================
# CONVEX BACKEND ENVIRONMENT VARIABLES
# Set these via: npx convex env set KEY=value
# For production add: --prod flag
# ===========================================

# GitHub OAuth
# npx convex env set AUTH_GITHUB_ID=...
# npx convex env set AUTH_GITHUB_SECRET=...

# Cloudflare DNS
# npx convex env set CLOUDFLARE_API_TOKEN=...
# npx convex env set CLOUDFLARE_ZONE_ID=...
# npx convex env set CLOUDFLARE_ACCOUNT_ID=...

# Cloudflare Turnstile (CAPTCHA)
# npx convex env set CLOUDFLARE_TURNSTILE_SECRET_KEY=...  (REQUIRED in production)
# npx convex env set TURNSTILE_DEV_BYPASS=true            (DEV ONLY - never in production!)

# Mailgun Email
# npx convex env set MAILGUN_API_KEY=...
# npx convex env set MAILGUN_DOMAIN=...
# npx convex env set FROM_EMAIL=...

# Site Configuration
# npx convex env set SITE_URL=https://your-convex-deployment.convex.site
# npx convex env set ALLOWED_ADMIN_EMAILS=admin@example.com,other@example.com

# Logo Storage (optional - endpoints return 503 if not set)
# npx convex env set LOGO_LIGHT_STORAGE_ID=...
# npx convex env set LOGO_DARK_STORAGE_ID=...

# ===========================================
# PRODUCTION vs DEVELOPMENT
# ===========================================
# Development: npx convex env set KEY=value
# Production:  npx convex env set KEY=value --prod
#
# CRITICAL PRODUCTION SETTINGS:
# - ALLOWED_ADMIN_EMAILS must be set (controls who can become admin)
# - CLOUDFLARE_TURNSTILE_SECRET_KEY must be set (CAPTCHA fails without it)
# - Do NOT set TURNSTILE_DEV_BYPASS in production
# - Set VITE_ADMIN_ROUTE in Vercel environment variables
#
# Do NOT use VITE_ prefix for secrets as they get bundled into client code
